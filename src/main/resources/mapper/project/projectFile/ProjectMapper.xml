<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.pickitup.mapper.project.projectFile.ProjectMapper">

    <!-- 프로젝트 전체 목록   -->
    <select id="getList" resultType="projectVO">
        SELECT NUM, TITLE, CONTENT, COURSE, TERRAIN, POINT, APPROVAL, STARTTIME, ENDTIME, START_ADDRESS, END_ADDRESS, DISTANCE, QR, REGIST_DATE, UPDATE_DATE, STATUS, COMPANY_NUM
        FROM TBL_PROJECT
    </select>


     <!--  특정 유저가 신청한 프로젝트 목록 -->
    <select id="getUserProjectList" resultType="projectVO">
    SELECT NUM, TITLE, CONTENT, COURSE, TERRAIN, POINT, APPROVAL, STARTTIME, ENDTIME, START_ADDRESS, END_ADDRESS, DISTANCE, QR, REGIST_DATE, UPDATE_DATE, STATUS, COMPANY_NUM
    FROM TBL_PROJECT
    WHERE COMPANY_NUM = #{companyNum}
    </select>


    <!-- 프로젝트 상세 정보  -->
    <select id="getDetail" resultType="projectVO">
     SELECT tp.NUM, tp.TITLE, tp.CONTENT, tp.COURSE, tp.TERRAIN, tp.POINT, tp.START_ADDRESS, tp.END_ADDRESS, tp.DISTANCE, tp.QR, tp.REGIST_DATE, tp.UPDATE_DATE,
            tp.STATUS , tp.COMPANY_NUM , tp.JJIM_COUNT , tp.PROJECT_DATE, tp.COMPANY_COMMENT, tp.SUMMARY,
            tc.NAME
            FROM TBL_PROJECT tp
            INNER JOIN TBL_COMPANY tc
            ON tp.COMPANY_NUM = tc.NUM
            WHERE tp.NUM = #{num}
    </select>


    <!-- 프로젝트 등록  -->
    <insert id="insert" parameterType="projectVO">
    INSERT INTO TBL_PROJECT
    (NUM, TITLE, CONTENT, COURSE, TERRAIN, POINT, APPROVAL, START_ADDRESS, END_ADDRESS, DISTANCE, QR, REGIST_DATE, UPDATE_DATE, STATUS, COMPANY_NUM, JJIM_COUNT, PROJECT_DATE, COMPANY_COMMENT, SUMMARY)
    VALUES(SEQ_PROJECT.NEXTVAL, #{title}, #{content}, #{course}, #{terrain}, #{point}, '0', #{startAddress}, #{endAddress}, #{distance}, NULL, SYSDATE, SYSDATE, '1' , #{companyNum}, NULL, #{projectDate}, #{companyComment}, #{summary})
    </insert>


    <!-- 프로젝트 수정  -->
    <update id="update" parameterType="projectVO">
        UPDATE TBL_PROJECT
        SET
        TITLE = #{title}, CONTENT = #{content}, COURSE = #{course}, TERRAIN = #{terrain}, POINT = #{point}, APPROVAL = #{approval}, STARTTIME = #{startTime} , ENDTIME = #{endTime},
        START_ADDRESS = #{startAddress}, END_ADDRESS = #{endAddress}, DISTANCE = #{distance}, QR = #{qr}, UPDATE_DATE = SYSDATE, STATUS = #{status}
        WHERE NUM = #{num}
    </update>


    <!-- 프로젝트 삭제 -->
    <delete id="delete" parameterType="long">
       DELETE FROM TBL_PROJECT WHERE NUM = #{num}
    </delete>


</mapper>


















